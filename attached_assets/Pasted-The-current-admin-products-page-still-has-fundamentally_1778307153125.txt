The current admin products page still has fundamentally incorrect scrolling architecture.

The issue is NOT minor sticky/z-index tweaking anymore.

Please stop trying to patch the current structure with additional sticky fixes.

Instead, carefully rebuild ONLY the admin products page layout as a proper fixed workspace architecture similar to modern professional admin systems.

Current Problem:
Right now the entire page still scrolls, causing:

* sticky conflicts
* overlapping layers
* unstable scrolling behavior
* table headers disappearing behind category/search sections
* jerky rendering during infinite loading
* inconsistent mobile behavior

This happens because multiple sections share the same scroll context.

Correct Goal:
The ENTIRE page should remain fixed.

ONLY the product table body/list should scroll internally.

Everything else should remain permanently visible and stable.

Required Workspace Structure:

1. Fixed Navbar

* Completely fixed at top
* Never participates in scrolling
* No layout shifting

2. Fixed Toolbar Section
   Directly below navbar:

* Products title
* Product count
* Add button
* Search bar
* Category tabs

Requirements:

* Permanently fixed
* Compact spacing
* Stable positioning
* No sticky conflicts
* Proper background and z-index

3. Fixed Table Header
   Headers:

* Img
* Name
* Price
* Stock
* Actions

Requirements:

* Permanently visible
* Never scroll behind categories/search
* Separate from scrollable rows
* Proper border/background layering
* Stable alignment with rows

4. Scrollable Product Rows ONLY
   This should be the ONLY scrolling section.

Requirements:

* Only table body scrolls internally
* Infinite loading happens inside this scroll container
* Product rows scroll behind the fixed table header
* Smooth loading without layout shifting
* Prevent height recalculations during append
* Maintain smooth touch/mobile scrolling

5. Proper Layout Architecture
   Requirements:

* Use full viewport-height workspace structure
* Prevent body/page scrolling
* Use dedicated isolated scroll container for rows only
* Avoid nested sticky behavior entirely
* Eliminate scroll-context conflicts completely

6. Smooth Workspace Experience
   The final experience should feel:

* stable
* smooth
* compact
* predictable
* application-like
* productivity-focused

NOT like:

* a webpage with sticky elements fighting each other

7. Search Bar Improvement
   Requirements:

* Slightly increase search bar height
* Improve tap feel/padding
* Keep clean premium appearance

Important:

* Do NOT redesign the admin panel visually
* Do NOT add new dashboard widgets/features
* Do NOT break infinite loading
* Do NOT affect existing APIs
* Keep implementation lightweight and maintainable
* Focus entirely on scroll architecture correctness and admin usability

The final result should feel like a modern compact admin workspace where controls always remain visible and only product rows move smoothly underneath.
