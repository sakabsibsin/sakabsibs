Carefully analyze and fully stabilize the admin products listing page logic and filtering behavior without redesigning the page or changing the current workspace architecture.

Current issue:
The category filtering state and product fetching logic are behaving inconsistently.

Reproduction Flow:

1. Open:
   Admin → Products
2. Default category:
   “All”
   → products load correctly
3. Select another category
   → products load correctly
4. Select another category again
   → products load correctly
5. Go BACK to a previously selected category
   → “No products match your search” appears incorrectly
6. Go back to “All”
   → “No products added yet. Add your first product.” appears incorrectly

This indicates there are state synchronization or query caching issues in the filtering/fetching system.

Requirements:

1. Analyze Full Product Page Logic
   Before changing anything:

* Analyze:

  * category filter state
  * infinite loading state
  * query caching
  * pagination reset logic
  * search state
  * API requests
  * hasMore logic
  * product list merging/appending
  * query invalidation behavior

2. Fix Category Switching Logic
   Requirements:

* Switching categories should ALWAYS fetch correct products
* Returning to previous categories should work correctly
* Returning to “All” should always restore all products properly
* Prevent stale product arrays from persisting
* Prevent incorrect empty states from appearing

3. Reset Logic Properly
   When:

* category changes
* search changes
* filters change

Properly reset:

* page number
* product array
* hasMore state
* loading state
* observer state if needed

Prevent old query results from mixing with new ones.

4. Validate Infinite Loading Stability
   Requirements:

* Ensure infinite loading continues working after category changes
* Prevent broken fetch cycles
* Prevent stuck states
* Ensure scrolling/loading works consistently across all category transitions

5. Empty State Validation
   Requirements:

* Show “No products match your search” ONLY when truly no matching products exist
* Show “No products added yet” ONLY when database is actually empty
* Prevent false empty-state rendering

6. Full QA / Testing Pass
   Treat this like a stability-testing and bug-fixing pass.

Requirements:

* Test:

  * switching categories repeatedly
  * switching back to previous categories
  * returning to “All”
  * searching + category switching
  * infinite scrolling after filter changes
  * loading additional pages
  * mobile + desktop behavior

The page should feel:

* stable
* predictable
* smooth
* reliable
* production-ready

Important:

* Do NOT redesign the admin page
* Do NOT change the current workspace architecture
* Do NOT break infinite loading
* Keep implementation lightweight and maintainable
* Focus entirely on logic correctness, state consistency, and reliability

The final result should feel like a stable professional admin workspace with fully reliable filtering, loading, and category behavior.
