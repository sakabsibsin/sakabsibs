Carefully refine and stabilize the admin products management page without changing the current workspace architecture or redesigning the UI.

The major scrolling and workspace structure issues are now mostly solved.
Focus only on fixing the remaining refinement, loading, and alignment issues.

1. Fix “Manage Products” Quick Action Loading Issue
   Current issue:
   When navigating to the products page through:

* Quick Actions → Manage Products

the products sometimes fail to load properly.

However:

* products load correctly when entering through:

  * View All
  * Dashboard menu
  * Other product routes

Requirements:

* Analyze the navigation flow specifically for the Quick Action route
* Identify why initial product fetching sometimes does not trigger
* Check for:

  * stale query state
  * route state issues
  * query initialization problems
  * scroll container initialization timing
  * infinite loader startup issues
* Ensure products always load correctly regardless of entry point

Important:

* The products page itself is mostly working
* Fix the navigation/fetch initialization flow specifically

2. Refine Product Name Typography
   Current issue:
   Product names feel slightly too large/heavy inside the compact admin workspace.

Requirements:

* Slightly reduce product name font size
* Maintain readability
* Improve visual balance and density
* Keep the UI clean and premium
* Avoid making text too small

3. Fix Header & Row Alignment Consistency
   Current issue:
   Table headers and row values are not perfectly aligned.

Examples:

* Code header vs code values
* Category header vs category values
* Price header vs price values
* Stock header vs toggle alignment
* Actions header vs buttons

Requirements:

* Ensure headers and row values share identical alignment structure
* Vertically center all content consistently
* Stabilize column alignment across desktop and mobile
* Ensure sticky headers and rows align perfectly
* Remove visual shifting/misalignment

Focus on:

* spacing precision
* typography consistency
* flex/grid alignment correctness
* premium workspace feel

Important:

* Do NOT redesign the admin page
* Do NOT change the current isolated scrolling architecture
* Do NOT affect infinite loading stability
* Keep implementation lightweight and maintainable
* Focus only on refinement, consistency, and usability polish

The final result should feel like a clean, stable, professional admin workspace with reliable navigation, precise alignment, and polished interaction quality.
