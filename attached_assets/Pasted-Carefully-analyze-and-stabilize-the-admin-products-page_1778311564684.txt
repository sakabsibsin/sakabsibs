Carefully analyze and stabilize the admin products page infinite loading behavior and table layout alignment without redesigning the page or changing the current architecture.

The major workspace structure is now mostly correct, but there are still important refinement and stability issues affecting usability.

Before making changes:

* Analyze the current infinite loading implementation carefully
* Analyze the IntersectionObserver behavior inside the isolated table scroll container
* Analyze current table column sizing/alignment logic
* Analyze mobile spacing distribution across columns

Current Problems:

1. Infinite Loading Stability Issue
   Sometimes:

* first ~20 products load correctly
* scrolling reaches bottom
* next fetch does NOT trigger
* reloading the page fixes it temporarily

This indicates instability in the infinite loading trigger system.

Requirements:

* Identify the root cause properly
* Stabilize the IntersectionObserver behavior
* Ensure loading always triggers reliably while scrolling
* Prevent observer disconnect/reconnect issues
* Prevent stale loading states
* Prevent hasMore/fetch state inconsistencies
* Ensure fetching works smoothly without requiring reloads

Important:

* Infinite loading should feel seamless and predictable
* Loading should trigger slightly before absolute bottom
* Keep scrolling smooth and lightweight

2. Mobile Column Spacing Optimization
   Current issue:
   Stock, Price, and Actions columns take too much horizontal space on mobile, causing the product name area to feel cramped.

Requirements:

* Reduce spacing/padding for:

  * Stock
  * Price
  * Actions
* Give more available width to the product name column
* Keep touch usability intact
* Maintain clean readability
* Keep layout compact and premium on mobile

3. Desktop Table Alignment Issues
   Current issue:
   Headers and columns are not aligning perfectly across:

* Code
* Category
* Price
* Stock
* Actions

Requirements:

* Make table columns align consistently
* Ensure sticky headers and body rows share identical column structure
* Prevent shifting/misalignment during scrolling
* Stabilize column widths properly
* Ensure professional clean table appearance

4. Scroll & Rendering Stability
   Requirements:

* Prevent visual jerking while loading more products
* Prevent height recalculations during append
* Maintain smooth scrolling performance
* Keep sticky header alignment stable during fetch/render cycles

Important:

* Do NOT redesign the admin page
* Do NOT change the current workspace architecture
* Do NOT break the isolated table scrolling system
* Keep implementation lightweight and maintainable
* Focus entirely on refinement, stability, alignment, and smooth admin usability

The final result should feel like a stable, polished, compact professional admin workspace with reliable infinite loading and clean table alignment.
