Carefully implement a scalable paginated fetching and infinite loading system across the entire ShopFlo project without redesigning the UI, breaking existing functionality, or overengineering the architecture.

Before making changes:

* Analyze all current product-fetching flows
* Identify where products are currently fetched fully
* Refactor the fetching system carefully while maintaining compatibility with existing features and APIs

This is a foundational architecture and performance update for the project and should be implemented cleanly and consistently.

Project Context:
This platform is designed for Instagram and WhatsApp sellers who may eventually manage 200–500+ products.

The goal is to keep the platform:

* lightweight
* smooth
* mobile-friendly
* scalable
* beginner-friendly
* premium feeling without excessive animations or complexity

Core Goal:
Do NOT fetch all products upfront anymore.

Instead:

* fetch products in small batches
* progressively load more while scrolling
* keep scrolling smooth and lightweight
* improve perceived speed and scalability across the entire project

Requirements:

1. Standardize Product Fetching Across Entire Project
   Implement paginated fetching consistently across:

* Homepage product sections
* Collection/category sections
* Product listing pages
* Admin product management pages
* Any other product-fetching areas

Ensure all product lists follow the same scalable fetching structure.

2. Backend Pagination System
   Update backend APIs carefully.

Requirements:

* Add:

  * page
  * limit
    support
* Default limit:
  20 products
* Return proper metadata:

  * currentPage
  * hasMore
  * totalProducts if needed

Implementation Notes:

* Use simple and maintainable MongoDB pagination
* Keep architecture lightweight
* Extend existing APIs safely
* Do not break current frontend integrations

3. Frontend Infinite Loading
   Implement smooth infinite loading behavior.

Requirements:

* Initially load only first 20 products
* Automatically load next products while scrolling
* Prevent duplicate API requests
* Prevent unnecessary re-renders
* Use efficient intersection observer approach
* Maintain smooth scroll performance

Important:

* Avoid jerky loading behavior
* Avoid layout shifting
* Keep loading visually subtle and smooth

4. Minimal Premium Loading Experience
   Add lightweight modern loading states.

Requirements:

* Skeleton product placeholders
* Subtle fade-in when new products appear
* Smooth append transitions
* Consistent loading spacing

Do NOT add:

* flashy animations
* heavy motion effects
* bouncing loaders
* complicated transitions

The experience should feel:
simple, smooth, modern, and premium.

5. Mobile Performance Optimization
   This project is heavily mobile-focused.

Requirements:

* Keep initial page load lightweight
* Improve scrolling smoothness on mobile
* Avoid memory-heavy rendering
* Prevent UI lag while loading more products
* Ensure touch scrolling remains responsive

6. Admin Product Page Performance
   Admins may manage hundreds of products.

Requirements:

* Keep admin product management fast and responsive
* Maintain sticky controls/header behavior properly
* Ensure infinite loading does not interfere with admin workflow
* Keep product actions/editing smooth and accessible

7. Stability & Maintainability
   Important:

* Do not redesign the project
* Do not affect existing responsive behavior negatively
* Do not break current product management functionality
* Reuse existing services/components wherever possible
* Keep implementation clean, simple, and maintainable

Focus on:

* scalability
* smooth UX
* simplicity
* maintainability
* perceived speed
* lightweight architecture

The final result should make the entire project feel significantly faster, cleaner, and more scalable while still remaining simple and beginner-friendly for non-technical sellers.
