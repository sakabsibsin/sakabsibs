Carefully analyze and restructure ONLY the admin product listing page layout and sticky scrolling behavior without redesigning the rest of the project or breaking existing functionality.

Before making changes:

* Analyze the current sticky hierarchy
* Analyze current scroll container behavior
* Analyze z-index layering and layout structure
* Identify why the table header scrolls behind the categories/search section
* Identify any fetching/rendering issues affecting scroll stability

Current Problems:

* Table header scrolls behind category tabs/search area
* Sticky behavior is structurally incorrect
* Layout hierarchy feels unstable
* Infinite loading causes slight visual instability
* The page does not feel like a proper admin workspace
* Too many elements share the same scroll context

Goal:
Make the admin product page feel like a clean, stable, professional workspace where:

* controls always remain visible
* table headers remain visible
* only product rows scroll
* scrolling feels smooth and predictable
* admins can manage products easily without losing context

Required Layout Architecture:

1. Sticky Navbar Layer

* Navbar remains stable at top
* No jerking or layout shifting

2. Sticky Toolbar Layer
   This section should remain sticky below navbar:

* Products title
* Product count
* Add button
* Search bar
* Category tabs

Requirements:

* Stable sticky positioning
* Proper z-index/background
* No overlapping issues
* Compact and clean spacing

3. Sticky Table Header Layer
   The table header must remain sticky BELOW the toolbar.

Headers:

* Img
* Name
* Price
* Stock
* Actions

Important:

* Table header should NEVER scroll behind category tabs
* Header should remain visible at all times
* Product rows should scroll underneath the table header only

4. Scrollable Product Rows ONLY
   Requirements:

* Only the product list/table body should scroll
* Keep infinite loading smooth inside the scrollable area
* Prevent layout shifting while loading more products
* Prevent sticky conflicts during fetch/render updates
* Ensure smooth touch/mobile scrolling

5. Search Bar Improvements
   Requirements:

* Slightly increase search bar height
* Improve internal padding and tap feel
* Keep design minimal and premium
* Do not oversize the component

6. Mobile Workspace Optimization
   This page is heavily used by admins on mobile devices.

Requirements:

* Reduce unnecessary spacing
* Improve workspace efficiency
* Keep important controls always accessible
* Ensure scrolling feels smooth and lightweight
* Keep the page compact but readable

7. Stability & UX Focus
   Focus heavily on:

* sticky hierarchy correctness
* z-index layering
* scroll container architecture
* smooth rendering
* predictable behavior
* workspace-like experience

Important:

* Do not redesign the entire admin panel
* Do not add unnecessary dashboard widgets/features
* Do not break infinite loading implementation
* Do not affect other pages negatively
* Keep implementation lightweight and maintainable
* Reuse existing architecture/components wherever possible

The final result should feel like a stable, compact, modern admin workspace built for non-technical sellers who need fast and frictionless product management.
